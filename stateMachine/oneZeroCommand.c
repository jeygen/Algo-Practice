/*
These commands (a `0' or a `1' on a line by itself) must print to stdout the name of the next state given 
the supplied input, and update the current state.
*/

#include <stdio.h>
#include <stdlib.h>
#include "commands.h"