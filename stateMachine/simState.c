#include <stdio.h>
#include <stdlib.h>
#include "commands.h"

typedef struct state_tag {
    char name;
    char zeroState;
    char oneState;
    int alive;
} state_t;

int main(int argc, char * argv[]) {
    exit(0);
}

